def run_job() -> None:
    # TODO: Read Bronze partitions, apply type cleanup + quality rules + dedupe,
    # and publish curated Silver dataset.
    print("[STUB] spark_bronze_to_silver")


if __name__ == "__main__":
    run_job()
